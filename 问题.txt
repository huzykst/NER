1、词向量使用时要不要更新

2、epoch、batch_size和keep_prob为什么不是每次使用时单独指定

3、以前只学过全连接用 drouout，没试过对LSTM的输入dropout

4、项目代码默认把句子填充到最长句子长度（l1），这样做的效果和选择一个大多数句子都小于的长度(l2)比较，哪个效果好（l1 > l2）

5、项目代码在LSTM的全连接层前后做了2次reshape，感觉是多余操作，直接接全连接也能得到相同的维度

6、什么情况下需要给tensor取名字